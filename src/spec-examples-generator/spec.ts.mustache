declare var LIB_PATH: string | undefined;

import('../../spec-examples-generator/spec-jit-proxy').then((proxy) => {
  proxy.runTest('{{{description}}}', '{{{expectation}}}');
  import(`${LIB_PATH}/../../../ballet/src/specs/{{{spec}}}`);
});